#include <stdio.h>
/**
 *  * main - Entry point
 *   * Description: This program uses the printf function to print a message.
 *    * Return: Always return 0 (success)
 *     **/
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
